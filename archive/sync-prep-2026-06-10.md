# Sync Prep — 2026-06-10

> **目標**：讓 Jarek 理解問題 → 展示 deliverable 進度 → 認可方向 → 確認邊界。
> Jarek 沒看過我的提案和 eval 文件。先展示進展，再討論方向。

---

## GSoC Deliverable 進度總覽（會議開始前心裡有數）

| # | Core Task | Issue 要求 | 狀態 | 備註 |
|---|-----------|-----------|------|------|
| 1 | Environment Detection | 偵測 host vs container，決策邏輯 | **基礎版完成** | SKILL.md 裡有 `test -f /.dockerenv && echo BREEZE=${BREEZE}`，只在 host 側驗過，未在 Breeze 內測試 |
| 2 | Workflow Skills | Static checks / unit tests / system verification | **部分完成** | 有一個 airflow-contribution skill（SKILL.md + template），包含 static checks、test runner 選擇、breeze suite 指令。尚缺 system verification（stretch goal） |
| 3 | Sync via prek | 從 source of truth 生成 skill，drift detection | **PR 已提交** | `feat/agent-skill-generation`: prek hook 從 contributing-docs RST 同步 AGENTS.md Commands section。Review feedback 待修（缺 additional_dependencies） |
| 4 | Evaluation | Testable user scenario / exam | **Prototype 完成** | 3 輪 iteration eval（with/without skill 對照），13 個 case。發現 one-shot baseline 已經很強（0.80-0.93），skill delta 只有 +0.07 |
| 5 | Documentation | 更新 AGENTS.md、Breeze docs | **未開始** | AGENTS.md 瘦身計畫有，但還沒動 |

---

## 開場（2 分鐘）：進度摘要

> Here's where I am on the GSoC deliverables.
> Task 3 (sync via prek) has a PR ready — it uses contributor-docs
> as the single source of truth, which is what we agreed on.
> Task 4 (eval) I ran 3 rounds of prototype evals and found
> something interesting — I'd like to walk you through it.
> Task 1 and 2 have a basic implementation in the skill file,
> but I want to make sure my direction is right before going deeper.

## Part 1（5 分鐘）：Eval 發現

### 具體例子

AGENTS.md line 26: "Never run pytest directly on the host — always use breeze."
AGENTS.md line 35+: 大量 `uv run --project <PROJECT> pytest ...`

→ 實測觀察：agent 引用 line 26 合理化用 breeze 跑本來該 uv run 的測試。

### 3 輪 eval 結果

| Iteration | 結果 | 觀察 |
|-----------|------|------|
| iter 1 (3 cases) | with-skill 1.0 vs without 0.6 | Skill 在甜蜜點 case 有效 |
| iter 2 (5 cases) | delta 只剩 +0.07 | 擴展後 baseline 已經 0.80-0.93 |
| iter 3 (5 cases) | delta +0.07 | 大部分 case 都 non-discriminating |

### 關鍵觀察

**One-shot task 模型表現已經很好。** Skill 在乾淨 context 下的增量價值很小。

## Part 2（5 分鐘）：假說和下一步

### 假說

問題不在 skill 內容品質，而在 **長 session 下 context 堆疊導致規則被遺忘**。
文獻支持存在（SlopCodeBench、Lost in the Middle），但我還沒有 Airflow 的實測數據。

### 我打算怎麼驗證

手動跑一個最小實驗：
- 開 Claude Code session，讓 agent 讀程式碼、做 code review 累積 context
- 在 turn 6 和 turn 11 問同一個 probe question（「幫我跑 test_taskinstance.py 的測試」）
- 看 pass rate 有沒有隨 turn 數下降
- 3 次重複，一個下午可以跑完

**問 Jarek**：
- 你在 steward 裡有觀察到類似的衰退嗎？
- 這個假說值得追嗎？還是先把其他 deliverable 做完？

### Eval 框架方向

（**給 Jason 的背景**：steward repo 裡有一套 skill-evals 框架。
做法很簡單 — 從 SKILL.md 提取一段當 prompt，配上 mock input，
pipe 給 `claude -p`，拿回 JSON 跟正確答案比對。
Justin 兩週前剛 merge 了 field-aware grading（PR #370），
讓 prose 欄位可以用 Haiku 判語意而不是逐字比對。）

我想用同樣的 **fixture 格式**（step-config.json、report.md、expected.json），
但在 Airflow repo 維護自己的 runner。

**為什麼 fixture 格式保持一致：**
- Case 可以互通，未來可以合併或互相引用
- 不用重新發明 case 結構

**為什麼需要自己的 runner：**
- Airflow 需要測 AGENTS.md 跟 skill 共存時的干擾
  （用 `claude -p --bare` + `--append-system-prompt-file` 控制隔離等級）
- 未來需要多 turn 實驗來驗證 context 衰退（steward runner 是單 turn）
- Airflow 的 runner 選擇邏輯比 triage 分類複雜（uv / breeze / prek 三種路徑）

**問 Jarek / Jason**：
- Fixture 格式跟 steward 保持一致，runner 自己維護，這個方向 OK 嗎？
- Runner 放 Airflow repo 的哪裡？`dev/skill-evals/`？

## Part 3（3 分鐘）：短期 action items

不管假說驗不驗證，這些可以馬上做：

1. **修 AGENTS.md line 26 矛盾** — 一行改動，eval 已證明這造成錯誤
2. **修 PR review feedback** — 補 additional_dependencies，對齊 prek script 風格
3. **在 Breeze container 內驗證 environment detection** — 目前只在 host 測過

**問 Jarek**：
- Line 26 矛盾修復放在現有 PR 還是另開？
- Task 1 (env detection) 需要更 robust 的機制（helper script）還是 skill 裡一行 bash 就夠？

## Part 4（3 分鐘）：Magpie 邊界

Magpie 的 Pairing / Mentoring 是往貢獻者側擴展。我的工作也是貢獻者側。

**直接問**：
- Pairing / Mentoring 什麼時候開始？scope 是什麼？
- 我的 skill 用 `airflow-*` 前綴，不走 Magpie snapshot，OK？

---

## 準備清單

- [ ] 準備好 AGENTS.md line 26 vs line 35 的對比（可以快速 show）
- [ ] 準備好 iter 1 數據（with-skill 1.0 vs without 0.6）
- [ ] 準備好 iter 2-3 數據（baseline 0.80-0.93，delta +0.07）
- [ ] 知道 Justin 的 field-aware grading PR (#370) 的內容，可以引用
- [ ] 能口頭說出 GSoC 5 個 task 各自的狀態
- [ ] 計時 — 如果 Part 1-2 聊超過 15 分鐘，砍 Part 4，會後 async

---

## Takeaways & Action Items

- (fill during meeting)
